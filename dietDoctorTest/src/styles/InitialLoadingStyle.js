import { StyleSheet } from 'react-native';

const InitialLoadingStyle = StyleSheet.create({
  constainerView: {
    flex: 1,
  },
  ImageBackgroundStyle: {
    flex: 1,
    justifyContent: 'flex-end',
  },
});

export default InitialLoadingStyle;
